# retail_lakehouse

I built this to rebuild, in dbt, the same Bronze / Silver / Gold pattern I ran
in Fabric at UFA: raw extracts from two source ERPs, reconciled into one
governed set of facts and dimensions. It runs on dbt-core and DuckDB, so
there's no warehouse and no cloud account to stand up — the adapter is a
config file; the modelling is the point.

## The problem it models

The business is mid-migration between two ERPs. ERP A is SAP-shaped — `MATNR`,
`WERKS`, `MENGE`, a mandant column on every table. ERP B is Dynamics 365
F&O-shaped — `ItemNumber`, `SiteId`, `Quantity`. Around 20 SKUs exist in both
systems, so a naive union double-counts them.

Reconciling that is the actual work, and it happens in `int_products_unioned`:
union, rank by source precedence, keep one row per natural key. ERP B wins
because it's the go-forward system, and `source_system` stays on the row so
anyone reading the mart can see which master a product came from. Sales get
the same treatment in `int_sales_unioned`, with the reasoning for that call
written as a comment in the model — overlap on transactions isn't the same
problem as overlap on master records, and it's worth checking the data before
assuming it is.

## Layers

| Layer | Materialisation | Does |
|---|---|---|
| `seeds/` | table | Raw extracts. Stands in for warehouse sources. |
| `models/staging/` | view | Rename, recast, drop. No joins, no logic. One model per source table. |
| `models/intermediate/` | ephemeral | Reconciliation and reshaping between staging and marts. |
| `models/marts/` | table | Facts and dimensions. What a BI tool connects to. |

## Running it

```bash
pip install dbt-core dbt-duckdb
dbt deps
dbt build --profiles-dir .
```

`dbt build` runs seeds, models and tests in dependency order and stops on
failure. That's also what `.github/workflows/dbt-build.yml` runs on every
push and PR, so a broken model or a failing test can't merge quietly. To
browse the lineage graph locally:

```bash
dbt docs generate --profiles-dir .
dbt docs serve
```

Or skip the local step: `.github/workflows/dbt-docs.yml` regenerates the docs
and publishes them to GitHub Pages on every push to master —
**[nolan-ruz.github.io/retail_lakehouse](https://nolan-ruz.github.io/retail_lakehouse/)**.

### Note on `packages.yml`

`packages.yml` currently pulls dbt_utils from GitHub rather than from
`hub.getdbt.com`, because the machine it was scaffolded on blocked the hub. On
a normal network use the standard form, which is commented at the top of the
file.

### Note on seeds vs sources

Raw data is seeded so the project runs anywhere. In a real project those would
be warehouse tables declared as `sources` with freshness thresholds, and
staging would call `{{ source(...) }}` instead of `{{ ref(...) }}`. Everything
downstream is identical. `seeds/_seeds.yml` carries the source declaration it
would become.

## Data

Synthetic, generated by `gen_seeds.py`. Two years of daily sales across six
prairie farm-supply branches and 120 SKUs, with a spring seeding peak and a
smaller autumn peak on seasonal categories, plus month-end inventory snapshots
carrying the min/max levels the supply chain team maintains by hand.

Seasonality is deliberate: it's what makes a forecast worth building, and
what makes `is_seasonal` belong in the dimension rather than being re-derived
in every report.

## Status

Done: all five marts are built and tested, `fact_sales` is incremental,
`snap_product_costs` snapshots cost history, and CI runs the build and
publishes the docs on every push.
