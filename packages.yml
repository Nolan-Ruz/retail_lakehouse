# Standard form (works on any normal network):
#   - package: dbt-labs/dbt_utils
#     version: [">=1.1.0", "<2.0.0"]
# Git form, used here because this sandbox blocks hub.getdbt.com:
packages:
  - git: "https://github.com/dbt-labs/dbt-utils.git"
    revision: 1.3.0
